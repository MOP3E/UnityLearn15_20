using UnityEngine;

namespace SpaceShooter
{
    /// <summary>
    /// Ограничение координат объекта координатами экрана.
    /// </summary>
    public class Bounds : MonoBehaviour
    {
        /// <summary>
        /// Start запускается перед первым кадром.
        /// </summary>
        private void Start()
        {
        
        }

        /// <summary>
        /// Update запускается каждый кадр.
        /// </summary>
        private void Update()
        {
        
        }
    }
}
